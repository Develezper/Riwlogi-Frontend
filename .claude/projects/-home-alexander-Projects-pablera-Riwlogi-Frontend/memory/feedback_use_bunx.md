---
name: Use bunx instead of npx
description: User prefers bunx over npx for running CLI tools
type: feedback
---

Use `bunx` instead of `npx` for running CLI tools (eslint, vite, etc).

**Why:** User preference — project uses bun as the package manager.

**How to apply:** Always use `bunx` or `bun run` instead of `npx` when executing CLI commands.
